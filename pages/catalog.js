import Layout from "../components/layout";

export default function Catalog() {
    return (
      <Layout>
        Estamos renovando el catalogo, por favor visitanos mas tarde
      </Layout>
    );
  }